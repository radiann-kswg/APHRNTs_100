[Unit]
Description=Run APHRNTs_100 auto-deploy check every 5 minutes

[Timer]
OnBootSec=2min
OnUnitActiveSec=5min
# 共用VM（misskey-bots-unified）では同居Botのジョブ・OSの定期処理と発火が重なりうる。
# 0〜60秒の揺らぎを与えて、毎回きっかり同じ秒に集中するのを避ける。
RandomizedDelaySec=60
Persistent=true

[Install]
WantedBy=timers.target
